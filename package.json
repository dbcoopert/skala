{
  "name": "skala",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "start": "cd backend && npm start",
    "build": "cd backend && npm install && npm run build:css"
  }
}
